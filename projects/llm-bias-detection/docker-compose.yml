services:
  llm-bias-detection:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-bias-detection
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
